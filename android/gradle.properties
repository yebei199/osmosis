# No AndroidX dependencies are used (plain framework APIs only), but AGP 8
# requires the flag to be declared.
android.useAndroidX=true
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.daemon=false
