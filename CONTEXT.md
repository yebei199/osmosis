# 术语表

本项目的通用语言(ubiquitous language)。只记术语的**含义与边界**,不记实现方式与技术选型 ——
那些属于 `docs/adr/`。

## 端 (Platform)

应用最终运行的一个目标环境。共六个:web、linux、windows、macOS、iOS、android。

"端"不等于"操作系统":web 是一个端,尽管它可以跑在任意操作系统上。区分依据是
**运行时能力与打包形态**,而非厂商。

当前已实现:android、linux。其余四端的结构位置已预留,尚未实现。

## 平台入口 (Platform Entry)

一个端启动应用所必需的、且**只对该端有意义**的那段代码。例如 android 的
`android_main`、桌面的 `fn main`、web 的 wasm 导出函数。

平台入口的职责被严格限定为三件事:初始化日志、初始化渲染后端、把控制权交给 UI 层。
**任何被两个及以上端需要的代码,按定义就不属于平台入口。**

## 契约 (Contract)

客户端与服务端之间在网络上传输的数据的**形状**:请求体、响应体、错误码、协议版本号。

契约刻意**不包含**领域规则。"一个订单不能被取消两次"是领域规则,不是契约;
"取消请求携带一个订单 id 字段"才是契约。契约是双方都必须同意的最小集合,
把领域规则塞进契约会迫使两侧的领域模型耦合演进。

## 客户端领域 (Core)

客户端持有的状态,以及改变该状态的规则。它是**客户端自己的**领域模型,不是服务端领域模型
的镜像 —— 二者只在契约处相遇。

客户端领域不知道自己被画成了什么样子(不知道 UI 的存在),也不知道数据是怎么拿到的
(不知道 HTTP、不知道 socket)。它知道"用户信息正在加载中"这件事,但不知道加载是靠
什么完成的。

## API 客户端 (Api)

把"取一份用户信息"这样的**意图**,翻译成一次具体的网络往返,并把结果翻译回契约类型。

API 客户端是各端运行时能力差异(有无线程、有无文件系统、能否阻塞)被吸收的地方 ——
差异到此为止,不再向上传播。

客户端领域**不认识** API 客户端。二者由 UI 层接起来:UI 层把 API 客户端的请求函数
交给客户端领域去调用。这个方向上的无知是刻意的 —— 它让客户端领域可以在没有网络的
情况下被完整地测试。

## UI 层 (Ui)

界面的声明,以及界面与客户端领域之间的双向绑定。

UI 层依赖客户端领域;**反向依赖被永久禁止**。这条方向性是本项目最核心的约束:
一旦客户端领域感知到 UI,它就无法被单独测试,也无法在不同端上被不同的界面复用。

## 版式 (Layout Mode)

界面在**一个时刻**所采用的整体排布形态。共两种:

- **紧凑版式**:导航在底部,内容占满整个宽度。
- **宽版式**:导航在左侧竖栏,内容占剩下的宽度。

版式**由视口宽度决定,与「端」正交**。同一个端在不同时刻可以是不同的版式:桌面把窗口
拖窄就是紧凑版式,手机横过来就是宽版式。二者不是同一个维度的概念 —— 「端」讲的是**运行时
能力与打包形态**(有没有文件系统、怎么装),版式讲的是**此刻有多少横向空间可用**。

把版式当成端的属性(「安卓 = 底部导航」)是个诱人的错误:它让平板、折叠屏、桌面窄窗全部
判错,并且让移动端的排布在桌面上永远无法被看见、无法被验证。见
[`docs/adr/0007`](docs/adr/0007-layout-mode-by-width-not-by-platform.md)。

## 设备 (Device)

用户的一台正在运行本应用的终端,如「小米13」「thinkpad」。由设备自己首次启动时生成并
本地保存的 id 标识,附带一个人读的名字。

设备与「端」正交:两台安卓手机是两个设备、同一个端。设备的在线与否**等于**它与服务端
之间是否存在活跃连接 —— 服务端不记忆离线设备。

**不是** bang-dream 术语表里的「设备标识 (Device ID)」:那是伪装音乐平台官方客户端用的
指纹,与本概念无关,讨论时不可混用。

_不要用_: 客户端(指代码分层)、端(是另一个维度)、终端

## 同播 (SyncPlay)

多台设备同时播放同一首歌、进度一致的能力。声音由主控实时送达听众,因此对齐是天然的,
不依赖时钟同步。

_不要用_: 一起听(隐含多人社交,这里全是同一人的设备)、广播(把实现形态烧进词里)

## 同播会话 (SyncSession)

一次同播从发起到解散的全过程,由一个主控和若干听众组成。主控退出或断线,会话即终结,
听众回到各自的单机状态 —— 会话不存在没有主控的形态。

_不要用_: 房间、派对(都隐含多人语义)

## 主控 (Host)

同播会话中作为声音来源的那台设备。它取得歌曲、解码、把音频送给每个听众;播放控制
(暂停、切歌、拖进度)只属于主控。一个会话恰有一个主控,主控身份在会话内不转移。

_不要用_: 发送端、sender(是传输角色,不含控制权语义)、房主

## 听众 (Listener)

同播会话中接收并播出主控所送音频的设备。留在会话里时,听众唯一的本地自由是音量 ——
它不进网络,不影响其他设备。但听众随时可以**退出**:在本机做任何播放动作
(播放/暂停、切歌、点歌)都视为退出收听、回到单机状态,不需要征得主控同意。

_不要用_: 接收端、receiver、成员

## 账号 (Account)

本应用的一个用户,由用户名标识,持有自己的本地歌单、播放事件,以及一份网易云凭据
绑定(见 `docs/adr/0017`)。账号回答「数据归谁」。

与「设备」正交:一个账号可以在多台设备上登录,设备回答的是「同播里推给哪台」。
也**不是**网易云账号:那是账号名下的一个绑定,生命周期各自独立。

_不要用_: 用户(口语易与「使用者本人」混淆,模型里统一叫账号)、会员

## 歌单 (Playlist)

一个有名字、可持久保存的曲目集合。按真相所在地分两个来源(见 `docs/adr/0016`):

- **平台歌单**:真相在音乐平台(网易云),这边只读与收藏/取消收藏。
- **本地歌单**:真相在自家 Postgres,可建、可改名、可删、可增删曲目,平台不知道它存在。

客户端统一呈现成一张列表,条目带来源标记;操作的可用性由来源决定。

_不要用_: 播放列表(是「队列」那个概念的错误叫法)、收藏夹

## 我喜欢的 (Liked)

一个特殊歌单:它**就是**平台的红心列表,不建本地副本。在歌单列表里置顶、不可删、
不可改名;红心/取消红心即是它唯一的增删方式。

自家 Postgres 里有它的一份**缓存**(见「缓存」词条)—— 那不是副本:红心仍然
只能改在平台上,缓存跟着走。

它的次序按「加入时间」倒序,最近点的红心在最前(见「加入时间」词条)。这与
平台红心接口给出的数组顺序不是一回事 —— 那个顺序稳定,但不表示任何东西。

_不要用_: 红心列表(那是平台侧的叫法,UI 与文档统一用「我喜欢的」)、收藏

## 缓存 (Cache)

平台曲目在自家 Postgres 里的一份留存,为的是不必每次进歌单都向平台重取
(见 `docs/adr/0018`)。

与「副本」的差别是三条,缺一条它就变回副本:写永远直发平台、冲突时平台赢、
整张删掉只是慢一次而不丢东西。据此可以问一个判定问题 —— **删了会丢数据吗**?
会,那它已经不是缓存了。

_不要用_: 镜像、副本(两者都隐含「这边也是一份真相」)

## 加入时间 (Added At)

一首歌被加进某个歌单的时刻,由平台给出,不是这边记的。歌单默认按它倒序排。

它属于**歌单的经营者**,不属于看歌单的人 —— 订阅别人的歌单时,这个时刻是原
作者往里加曲目的时刻。所以按它排序,对自建歌单意思是「我最近收藏的」,对订阅
来的歌单意思是「原作者最近加的」,两者不是同一件事,而界面上看起来一样。

_不要用_: 收藏时间(听起来像看歌单的人的动作,而它未必是)、创建时间

## 播放事件 (Play Event)

「某账号在某时刻起播了某首歌」的一条不可变记录,起播即追加,不补记听了多久。
播放历史与听歌统计都是对事件流的查询时聚合,不存在独立的统计数据。

_不要用_: 播放记录(隐含可编辑)、打点/埋点(那是采集视角,不是领域概念)

## 队列 (Queue)

当前正在播放的那**一批**歌与其中的位置。批就是音乐页上最近一次装进列表的东西
(一次搜索的结果、每日推荐、红心列表、某个歌单的曲目),换一批就整个换队列。上一首/下一首/随机
播放都在队列内进行;随机是对整批洗一次牌,一轮内不重复,放完即停,不自作主张
再来一轮。

**随机开着没有,是队列自己记着的那一位**。控制条上那个开关、系统媒体控件上
那个开关,都只是它的投影 —— 谁拨的不重要,变的是同一位。换一批把随机清掉:
新批还没洗过,说它是随机的就是撒谎。

当前这首站稳之后,队列里的**下一首**会被提前备好(取直链 + 开流 + 解码),
于是按「下一首」时声音立刻就换。备的那一份认它自己那一首 —— 中途改主意
(点了别的歌、洗了牌)就地丢掉,不会放出一首没点过的歌。

预取不成、或点的是列表里任意一首时,**界面先走**:旧歌立刻停,新歌名与封面
立刻上,播放页显示加载态,声音几秒后进来。屏幕说什么,耳朵就该听什么 ——
让旧歌继续放着而界面已经换了,是自相矛盾。

_不要用_: 播放列表、歌单(那是可持久保存的曲目集合,是队列的**来源**之一,
不是队列本身 —— 见「歌单」词条)

## 跳转 (Seek)

把播放位置挪到这一首里的另一个时刻。用户那头是拖进度条,底下是**一次请求,
不是一次调用** —— 能跳的解码器在缓冲通道的另一头,请求要穿过去交给解码线程执行。

因此「送出去了」与「跳成了」是两件事。当场就知道跳不动的(格式不支持、这条流
只进不退)当场报;真要重开一个 range 请求去取字节的,先放行、界面挂「缓冲中」,
结论随后取回(见 `docs/adr/0019`)。

**能不能往回跳取决于知不知道流的长度**。长度未知时这一首只进不退 —— 那是解码器
的限制,不是网络的。

_不要用_: 快进/快退(那是变速播放的说法,这里是**跳到某一点**,中间的声音不放)、
缓冲(那是跳转之后可能进入的状态,不是跳转本身)

## 断流 (Stream Loss)

一首歌**没放完就再也拿不到声音**的那个结局。与「放完了」并列,两者都让播放器空下来,
但该做的事相反:放完了切下一首,断流停在原地并且开口说话(见 `docs/adr/0013`)。

判据是下载侧连续重连仍拿不到数据,不是「听不见声音」—— 缓冲垫空时给的静音也听不见,
那只是抖动,会自己缓过来。断流是已经放弃了。

用户看到的是一条跨页横幅。文案先说发生了什么(播放中断),探明之后再说是哪一种:
本机没网,还是这条播放地址不行了。

_不要用_: 断网(只是断流的一种成因,直链过期、CDN 挂掉同样会断流)、加载失败
(那是起播前就没成功,用户还没听到过声音,走的是另一条路)

## 系统媒体控件 (Media Controls)

**宿主系统**替本应用摆出来的那套播放界面:Linux 桌面外壳的媒体卡片、安卓的锁屏与
通知栏。它同时是两个方向 —— 把此刻在放什么报出去,并接住外面按下的键。

各端的说法不同(Linux 是 session bus 上的 MPRIS,安卓是 `MediaSession` 加一条挂在
前台服务上的通知),要的东西是同一样。所以接缝在 UI 层,方言在平台入口
(`docs/adr/0020`)。

它不是应用自己的控制条 —— 控制条是本应用画的,媒体控件是系统画的。两者显示同一份
状态,但控制条能读到的东西比媒体控件多得多:媒体控件只知道歌名、艺术家、封面、
时长、位置、放没放、随机开没开这几样,不知道队列里都有谁、不知道歌词、
不知道同播。

_不要用_: 会话(那是同播会话或登录会话)、主控(那是同播里推流的那一端)

## 播放页 (Play Page)

从控制条展开的全窗沉浸层。它是「正在播放」这个**状态的展开形态**,不是导航
目的地 —— 不占导航位,导航仍然只有 Home 与 Music 两个 tab。

出口是**控制簇里的 ▼**,安卓的返回键与桌面的 Esc 同样收起它 —— 返回先离开这一层,
再按才离开应用。点击页面本身不收起 —— 那是在拨弄粒子。

页面职责是氛围与沉浸,不是效率;操作类功能(播/暂/切/进度)由常驻控制条承载,
播放页自身不新增任何**只能在这里完成**的操作。

_不要用_: 正在播放页(Now Playing,隐含系统媒体控件语义)、可视化页(可视化只是
它的一层,不是全部)、舞台(过度诗意,指代不清)

## 可视化 (Visualization)

播放页上跟随**本机正在播出的声音**而动的装饰层。数据永远取自本机解码后即将播出的
PCM —— 不区分单机、主控、听众,频谱不进网络、不进契约。

可视化是纯装饰:它可以遮挡界面元素,但被它遮挡的东西必须在别处有等价路径;它不
承载任何功能,消失时应用功能完好无损。

它只在「播放页展开 ∧ 正在播放 ∧ 窗口聚焦」时渲染,任一条件不满足即完全停止 ——
暂停定格在最后一帧,收起与失焦零重绘。

_不要用_: 频谱(那是它的输入数据,不是这一层本身)、特效(不含「跟着声音」的语义)

## 封面点云 (Cover Point Cloud)

可视化的主体形态:当前曲目的**封面被采样成一片粒子**,每颗取封面上对应位置的颜色,
整片随声音起伏。它是「封面这张图的另一种呈现」,不是飘在封面周围的装饰物 —— 没有
封面时它退回一片渐变色,而不是消失。

换歌时点云不重建,只换颜色:先退回渐变,新封面到了再过渡上去。**不留着上一首的图
等新图** —— 旧封面配新歌比空着更误导,与 2D 封面、歌词同一条原则。取封面要几百毫秒,
那段时间点云就是渐变色。

_不要用_: 粒子(泛指任何一颗一颗的东西,不含「来自封面」这层意思)、星空/尘埃
(曾经那层与封面无关的装饰,已拆除,再用会混淆)

## 视觉预设 (Visual Preset)

同一批粒子的另一种摆法。换预设**不重建粒子**,只是把同一批点摆到别处。它是
「同一样东西的另一个样子」,不是另一套视觉。

**当前只有「封面」一档。** 曾经的另外五档(管壁、球面、虚空、唱片盘、极光带)已删:
粒子离开封面平面之后那张图就散了,而点云存在的理由正是「封面的另一种呈现」
(见 `docs/adr/0014`)。入口与那条 seam 留着,加档时往列表里填即可。

预设只改观感,不改功能:换哪一档都不影响播放、歌词与遮挡的行为。

_不要用_: 主题(那是整个应用的明暗与配色,不是播放页这一层)、模式(隐含行为差异,
而预设只差摆法)
