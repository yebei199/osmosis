//! 后端的库部分:gRPC 客户端、领域翻译与错误映射。
//!
//! 单独立 lib 而不是全塞进 `main.rs`,是为了让 `tests/` 下的集成测试能 `use server::…`。
//! 集成测试引不到二进制 crate —— 用 `#[path]` 把源文件包含进去的话,
//! 模块里的单元测试会**再跑一遍**,同一个断言在报告里出现两次。
//!
//! HTTP 路由留在 `main.rs`:它们是这个进程的装配方式,不是可复用的库。

pub mod bangdream;
pub mod error;
