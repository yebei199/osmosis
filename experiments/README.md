# experiments

一次性的技术验证:为了回答一个具体问题、拿到一组数据而写的原型与测量脚本。
每个实验一个子目录,自带 README(要回答什么、怎么跑、结论在哪)。

不是产品代码:不进主 workspace、不进 `just ci`,产品 crate 不许依赖这里的任何东西。
结论被采纳后,要进产品的部分按产品的结构重新写,不从这里搬。

- `synctest/`:#137 ② 紧同步技术验证 —— 各设备独立拉流 + 共享时间轴,真实出声能对齐到多少。
