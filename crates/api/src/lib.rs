//! API 客户端:把"取一份用户信息"这样的意图,翻译成一次具体的网络往返。
//!
//! 这里是各端运行时能力差异(有无线程、有无文件系统、能否阻塞)被吸收的地方,
//! 差异到此为止,不再向上传播。对外暴露的 `async fn` 在 native 与 wasm 上
//! **签名完全相同**;`Send` 约束只存在于本 crate 内部。见 docs/adr/0002。
//!
//! 目前为空。native(reqwest)与 wasm(fetch)的两份实现随 issue #3 加入。
