# app-core/src/session

`session.rs`(播放组会话，遥控器那一侧)的测试，按阶段分文件:

- `tests.rs`:③ 的单成员迁移 —— 选一台设备就把当前播放整个迁过去，四步确认与「待确认」;
- `group_tests.rs`:⑤ 的多成员 —— 改在这些设备播放、加入、移出，显式主端交接，逐台待确认。

规则本身都在上一级的 `session.rs` 里。
