# app-core/src/group

`group.rs`(播放组，成员那一侧)的测试。规则本身在上一级的 `group.rs`:

- 收哪份共同计划(任期、序号、纪元)、预告的下一首到点就换、计划到头了停下;
- 主端怎么写计划(定锚、心跳、预告下一首、有效期)、交接后新主端怎么接着写;
- 主端多久没动静算失联。

遥控器那一侧(谁加入、谁移出、每一步的确认)在 `session.rs`。
