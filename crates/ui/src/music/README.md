# music

音乐页的绑定:搜索/每日/红心三路来源、队列点播、控制条、下一首的预取、
开机自检,以及播放页元数据的推送。显示格式化都在这一层做。

- `playback/`:放哪一首(控制条绑定、传输层、自动续播)。
- 其余:摆出哪些歌(`list`、`feed`)、下载(`download`)、文案与规则
  (`rules`、`report`、`notice`)。

不负责队列本身的规则 —— 那在 `app-core`;也不负责请求 —— 那在 `api`。
各子模块的条目都引进 `music.rs` 那一层,子模块因此能互相看见:拆分前它们
本就在同一个作用域里。
