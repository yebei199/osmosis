# playback

放哪一首,以及放的时候条上发生什么:控制条的绑定(`controls`)、起播与切歌的
传输层(`transport`)、自动续播与下一首的预取(`advance`)。

与 `../list`、`../feed` 的分法:那两个管「摆出哪些歌」,这一组管「放哪一首」。
解码与设备归 `crates/audio`,队列规则归 `app-core`。

跳转是异步的:请求送到解码线程后立刻挂「缓冲中」,落地与失败由每秒那趟轮询取回。
