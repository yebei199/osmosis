# runtime

帧循环这一侧:每帧组装参数、驱动各渲染器、把结果推回界面(`render_loop`),
一帧耗时的记账与可选的帧率读数(`frame_stats`),以及歌词推给界面前的去重
(`lyric_push`)。

不做任何业务判断 —— 推什么、什么时候换歌由各绑定模块定,这一层只管
「这一帧做多少事」。每帧无脑 set 会把属性标脏,去重就是为此。

前台恒满帧,后台由平台停发重绘而自然暂停;没有冻结门。
