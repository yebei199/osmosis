# sync

设备之间的界面状态:本机身份、信令连接与名册(`link`),以及播放组那一侧
(`group`:手上那份服务端全局状态、组意图的发送、横幅与组那一行的文案,`docs/adr/0032`)。

协议与传输在 `crates/syncplay`,这里只有界面这一层。目录名是同播留下的:同播
(WebRTC 推流)已删(#137),信令连接如今只承载名册、校时与组状态的广播。

整组只在原生 target 上编。
