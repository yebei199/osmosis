# imagery

封面字节变成 `slint::Image`,以及记住它。

三份分开是因为**键不同**:`cover` 只管解码(大图给播放页、96px 小图给列表),
`artwork` 按歌单 id 存,`thumbnail` 按封面 URL 存。键不同,缓存、去重与淘汰的
规则就全都不同 —— 一张专辑封面被十几首歌共用时只该取一次。

取字节那一步不在这里,归 `api`。整组只在原生 target 上编:解码要 image。
