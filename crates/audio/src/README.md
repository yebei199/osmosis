# audio/src

`audio` crate 的源码：把一条直链变成声音，包括取流、解码、缓冲、播出，以及派生出来的可视化数据。
逐个文件的说明见 crate 根目录的 `../README.md`。

子目录:

- `sync/`:同步播放。让媒体位置跟住一条共享时间线，多台设备一起出声(#137 ⑤)。
- `loader/`、`stream_source/`、`player/`、`pcm/`、`error/`:各自模块的测试，以及从模块里拆出去的部分。

不负责校时、组计划和「下一首是谁」:这些归 `syncplay` 与 `ui`。
