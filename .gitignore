# Rust
/target
/.docker-target
/target-android

# Build artifacts
/dist
# gradle 的产物目录会随工程位置变动,用 pattern 而非绝对路径
**/.gradle/
**/jniLibs/
/apps/android/gradle/app/build
/apps/android/gradle/build

# e2e(test/e2e 那套 bun/playwright)
node_modules/
/test/e2e/test-results

# IDE / misc
*.iml
.idea/
.vscode/
*.log
local.properties

# direnv cache
/.direnv

# 本地凭据:邀请码、测试账号。justfile 用 `set dotenv-load` 读它,
# 别提交 —— 服务面向公网,邀请码进了历史就等于没有邀请码
/.env
